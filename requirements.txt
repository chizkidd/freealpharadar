# FreeAlphaRadar — pinned dependencies for reproducibility.
# Zero API keys, zero secrets. Every data source is free and key-less.
#
# This is a known-good, mutually compatible stack (pandas 2.x / numpy 1.26 /
# torch 2.5 / transformers 4.46). FinBERT/torch/xgboost are heavyweight but
# optional at runtime: the app degrades gracefully to a lexicon sentiment
# backend and rule-based scoring if they are unavailable.

# ---- Core app & dashboard ----
streamlit==1.41.1
plotly==5.24.1
pandas==2.2.3
numpy==1.26.4

# ---- Free, key-less data sources ----
yfinance==0.2.51
requests==2.32.3
aiohttp==3.11.11
sec-edgar-downloader==5.0.3
beautifulsoup4==4.12.3
lxml==5.3.0
python-dateutil==2.9.0.post0

# ---- ML / NLP (zero-shot, pre-trained; optional at runtime) ----
scikit-learn==1.5.2
transformers==4.46.3
torch==2.5.1
xgboost==2.1.4

# ---- Dev / test / quality ----
pytest==8.3.4
black==24.10.0
isort==5.13.2
pre-commit==4.0.1
